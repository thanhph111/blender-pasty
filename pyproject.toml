[project]
name = "blender-pasty"
version = "0.0.0"
description = ""
readme = "README.md"
requires-python = "==3.11.*"
dependencies = []

[dependency-groups]
build = [
    "bpy>=4.5.1",
]
dev = [
    "fake-bpy-module>=20250630",
    "ruff>=0.12.7",
    "ty>=0.0.1a17",
]

[tool.uv]
environments = ["sys_platform == 'darwin'", "sys_platform == 'linux'"]

[tool.ruff]
extend-exclude = []
fix = true
line-length = 100

[tool.ruff.format]
skip-magic-trailing-comma = true

[tool.ruff.lint]
extend-select = [
    "B", # flake8-bugbear
    "C4", # flake8-comprehensions
    "ERA", # flake8-eradicate/eradicate
    "I", # isort
    "N", # pep8-naming
    "PIE", # flake8-pie
    "PGH", # pygrep
    "RUF", # ruff checks
    "SIM", # flake8-simplify
    "T20", # flake8-print
    "TCH", # flake8-type-checking
    "TID", # flake8-tidy-imports
    "UP", # pyupgrade
]
ignore = [
    "T201", # Don't check for print statements
    "ERA001", # Found commented-out code
    "N801", # Don't check class names
]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = [
    "N999", # Don't check root __init__.py files
]

[tool.ruff.lint.flake8-tidy-imports]
ban-relative-imports = "all"

[tool.ruff.lint.isort]
combine-as-imports = true
split-on-trailing-comma = false

[tool.pyright]
typeCheckingMode = "off"
